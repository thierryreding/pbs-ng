ARCH = $(arch)
ARCH_LONG = $(arch.generation)
ENDIAN = little

VENDOR = unknown
OS = linux
LIBC = gnu

HOST = $(ARCH_LONG)-$(VENDOR)-$(OS)-$(LIBC)
CFLAGS = -march=$(arch.generation) -mcpu=$(arch.cpu)

ARCH_LONGDOUBLE_FORMAT = INTEL_EXTENDED_16_BYTES_LE
