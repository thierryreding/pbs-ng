description: Video Acceleration (VA) API for Linux
version: 2.13.0
license: MIT

files:
  - download:
      url: https://github.com/intel/libva/releases/download/${version}/libva-${version}.tar.bz2
      sha1: 2adb2c023371dd68fe0d23e58afcf0b072b79828

depends:
  - libs/libdrm
  - libs/libglvnd

packages:
  libva:
    files:
      - /usr/lib/*.so.*

  libva-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
