description: Userspace tools and conversion library for Video4Linux
version: 1.18.0
license: GPL-2.0

files:
  - download:
      url: https://www.linuxtv.org/downloads/v4l-utils/v4l-utils-${version}.tar.bz2
      sha1: 3c3c9d4c9924f0153974ebcf8554331e04914050

depends:
  - devel/glibc

packages:
  libv4l:
    files:
      - /usr/lib/*.so.*
      - /usr/lib/libv4l

  libv4l-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  v4l-utils:
    files:
      - /etc
      - /usr/bin
      - /usr/lib/udev
      - /usr/sbin

# vim: et ft=yaml sts=2 sw=2 ts=2
