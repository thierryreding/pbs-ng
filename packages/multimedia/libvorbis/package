description: Vorbis codec library
version: 1.3.5
license: BSD

depends:
  - multimedia/libogg

files:
  - download:
      url: http://downloads.xiph.org/releases/vorbis/libvorbis-${version}.tar.xz
      sha1: 7b4cdd4a73fadfed457ae40984cb0cc91146b300

packages:
  libvorbis:
    files:
      - /usr/lib/*.so.*

  libvorbis-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
