description: Transport Independent RPC library (SunRPC replacement)
version: 1.0.2
license: BSD

depends:
  - devel/glibc

files:
  - download:
      url: http://prdownloads.sourceforge.net/libtirpc/libtirpc-${version}.tar.bz2
      sha1: 2a8dc0e6eecc45be6597c8287b1d8e15cbee46e3

packages:
  libtirpc:
    files:
      - /etc
      - /usr/lib/*.so.*

  libtirpc-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libtirpc-doc:
    files:
      - /usr/share/licenses

  libtirpc-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
