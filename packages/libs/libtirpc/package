description: Transport Independent RPC library (SunRPC replacement)
version: 1.2.6
license: BSD

depends:
  - devel/glibc

files:
  - download:
      url: http://prdownloads.sourceforge.net/libtirpc/libtirpc-${version}.tar.bz2
      sha1: f182235e32942fc0ac6b9b5b6fe7e32f69362659

packages:
  libtirpc:
    files:
      - /etc
      - /usr/lib/*.so.*

  libtirpc-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libtirpc-doc:
    files:
      - /usr/share/licenses

  libtirpc-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
