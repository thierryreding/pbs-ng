description: Transport Independent RPC library (SunRPC replacement)
version: 1.1.4
license: BSD

depends:
  - devel/glibc

files:
  - download:
      url: http://prdownloads.sourceforge.net/libtirpc/libtirpc-${version}.tar.bz2
      sha1: d85717035cb9bd6c45557a1eb1351d3af9a69ff7

packages:
  libtirpc:
    files:
      - /etc
      - /usr/lib/*.so.*

  libtirpc-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libtirpc-doc:
    files:
      - /usr/share/licenses

  libtirpc-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
