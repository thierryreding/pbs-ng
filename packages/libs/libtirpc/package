include: packages/libs/libtirpc/package.include

depends:
  - devel/glibc

packages:
  libtirpc:
    files:
      - /etc
      - /usr/lib/*.so.*

  libtirpc-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libtirpc-doc:
    files:
      - /usr/share/licenses

  libtirpc-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
