include: packages/libs/libevent/package.include

depends:
  - devel/glibc

packages:
  libevent:
    files:
      - /usr/lib/*.so.*

  libevent-dev:
    files:
      - /usr/bin
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
