description: implementation of the Stringprep, Punycode and IDNA specifications
version: 1.35
license: GPL-3.0, LPGL-2.1

files:
  - download:
      url: http://ftp.gnu.org/gnu/libidn/libidn-${version}.tar.gz
      sha1: d06a1e04caf5478cfb3ce525a83701a73b564fca

depends:
  - devel/glibc

packages:
  libidn:
    files:
      - /usr/lib/*.so.*

  libidn-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
