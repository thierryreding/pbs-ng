description: a C library that implements an SQL database engine
version: 3.53.2
license: custom

depends:
  - devel/glibc
  - libs/readline

files:
  - download:
      url: https://github.com/sqlite/sqlite/archive/refs/tags/version-${version}.tar.gz
      sha1: 95109bdd6d1231f66468ccfb6fde616e63b22904
      filename: sqlite-${version}.tar.gz
      watch:
        url: anitya://4877

packages:
  sqlite:
    files:
      - /usr/bin
      - /usr/lib/*.so.*

  sqlite-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: ft=yaml
