description: low-level cryptographic library
version: 3.10.2
license: GPL-2.0

depends:
  - libs/gmp

files:
  - download:
      url: https://ftp.gnu.org/gnu/nettle/nettle-${version}.tar.gz
      sha1: 7facbe118b985faab180cdcac13bebd16cc633b4
      watch:
        url: anitya://2073

packages:
  nettle:
    files:
      - /usr/lib/*.so.*

  nettle-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
