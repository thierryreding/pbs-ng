description: low-level cryptographic library
version: 3.5.1
license: GPL-2.0

depends:
  - devel/glibc

files:
  - download:
      url: https://ftp.gnu.org/gnu/nettle/nettle-${version}.tar.gz
      sha1: 077a71eef4a62acc4480ff7e2051f806b1724ea2

packages:
  nettle:
    files:
      - /usr/lib/*.so.*

  nettle-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
