description: A library for configuring and customizing font access
version: 2.18.1
license: custom

depends:
  - build-tools/meson
  - devel/glibc
  - libs/bzip2
  - libs/expat
  - libs/freetype
  - libs/json-c

files:
  - download:
      url: https://gitlab.freedesktop.org/api/v4/projects/890/packages/generic/fontconfig/${version}/fontconfig-${version}.tar.xz
      sha1: eb444d2cfc211c0d6281f9872539fb9db9c8188a
      watch:
        url: anitya://827

packages:
  libfontconfig:
    files:
      - /etc/fonts
      - /usr/lib/*.so.*
      - /usr/share/fontconfig
      - /usr/share/xml

  libfontconfig-bin:
    files:
      - /usr/bin

  libfontconfig-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libfontconfig-doc:
    files:
      - /usr/share/doc

  libfontconfig-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
