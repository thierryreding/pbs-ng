description: A library for configuring and customizing font access
version: 2.12.6
license: custom

depends:
  - devel/glibc
  - libs/expat
  - libs/freetype

files:
  - download:
      url: http://fontconfig.org/release/fontconfig-${version}.tar.gz
      sha1: d5de50c30317cf20e86c49b4aa25175e0ad9b8e8

packages:
  libfontconfig:
    files:
      - /etc/fonts
      - /usr/lib/*.so.*
      - /usr/share/fontconfig
      - /usr/share/xml

  libfontconfig-bin:
    files:
      - /usr/bin

  libfontconfig-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libfontconfig-doc:
    files:
      - /usr/share/doc

  libfontconfig-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
