description: a YAML parser and emitter in C++
version: 0.7.0
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://github.com/jbeder/yaml-cpp/archive/yaml-cpp-${version}.tar.gz
      sha1: 8a4126f1e21d374dac8dfc8524ffea101a160f2f

packages:
  yaml-cpp:
    files:
      - /usr/lib/*.so.*

  yaml-cpp-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/cmake
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
