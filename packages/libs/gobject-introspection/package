include: packages/libs/gobject-introspection/package.include

depends:
  - build-tools/gobject-introspection
  - build-tools/meson
  - build-tools/qemu
  - devel/python
  - libs/cairo
  - libs/glib
  - libs/libffi

packages:
  gobject-introspection:
    files:
      - /usr/lib/*.so.*

  gobject-introspection-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
