include: packages/libs/tbb/package.include

depends:
  - devel/glibc

packages:
  tbb:
    files:
      - /usr/lib/*.so.*

  tbb-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/cmake

# vim: et ft=yaml sts=2 sw=2 ts=2
