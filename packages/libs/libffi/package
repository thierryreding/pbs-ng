include: packages/libs/libffi/package.include

depends:
  - devel/glibc

packages:
  libffi:
    files:
      - /usr/lib/*.so.*

  libffi-dev:
    files:
      - /usr/include
      - /usr/lib/libffi-*
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libffi-doc:
    files:
      - /usr/share/info
      - /usr/share/licenses

  libffi-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
