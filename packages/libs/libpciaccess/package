description: X11 PCI access library
version: 0.13.5
license: custom

depends:
  - devel/glibc

files:
  - download:
      url: http://xorg.freedesktop.org/releases/individual/lib/libpciaccess-${version}.tar.bz2
      sha1: ea98c13623e218359ca6d9af9ab9aa4780201a5b

packages:
  libpciaccess:
    files:
      - /usr/lib/*.so.*

  libpciaccess-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
