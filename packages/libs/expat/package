include: packages/libs/expat/package.include

depends:
  - devel/glibc

packages:
  expat:
    files:
      - /usr/bin

  expat-man:
    files:
      - /usr/share/man

  libexpat:
    files:
      - /usr/lib/*.so.*

  libexpat-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/cmake
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
