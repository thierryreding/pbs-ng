diff --git a/glycin-loaders/meson.build b/glycin-loaders/meson.build
index 8e456ec391cc..577b0bdad65c 100644
--- a/glycin-loaders/meson.build
+++ b/glycin-loaders/meson.build
@@ -51,6 +51,10 @@ cargo_options = [
   '--manifest-path', manifest,
 ]
 
+if get_option('triplet') != ''
+  cargo_options += [ '--target', get_option('triplet') ]
+endif
+
 git_version = ''
 
 if cargo_profile != 'release'
@@ -93,6 +97,12 @@ foreach loader : get_option('loaders')
     ],
   )
 
+  if get_option('triplet') != ''
+    cargo_output = cargo_target_dir / get_option('triplet') / rust_target / loader
+  else
+    cargo_output = cargo_target_dir / rust_target / loader
+  endif
+
   custom_target(
     loader + '-cp-binary',
     depends: cargo_build,
@@ -103,7 +113,7 @@ foreach loader : get_option('loaders')
     output: loader,
     command: [
       'cp',
-      cargo_target_dir / rust_target / loader,
+      cargo_output,
       '@OUTPUT@',
     ],
   )
diff --git a/glycin-thumbnailer/meson.build b/glycin-thumbnailer/meson.build
index 44c6fbba3d0b..167219d8f37c 100644
--- a/glycin-thumbnailer/meson.build
+++ b/glycin-thumbnailer/meson.build
@@ -2,6 +2,12 @@ if not get_option('glycin-thumbnailer')
     subdir_done()
 endif
 
+if get_option('triplet') != ''
+  cargo_target_args = [ '--target', get_option('triplet') ]
+else
+  cargo_target_args = []
+endif
+
 cargo_build = custom_target(
     'glycin-thumbnailer',
     build_by_default: true,
@@ -16,9 +22,15 @@ cargo_build = custom_target(
         '--profile', cargo_profile,
         '--manifest-path', cargo_manifest,
         '--package=glycin-thumbnailer',
-    ],
+    ] + cargo_target_args,
 )
 
+if get_option('triplet') != ''
+  cargo_output = cargo_target_dir / get_option('triplet') / rust_target / 'glycin-thumbnailer'
+else
+  cargo_output = cargo_target_dir / rust_target / 'glycin-thumbnailer'
+endif
+
 custom_target(
     'glycin-thumbnailer-cp-binary',
     depends: cargo_build,
@@ -29,7 +41,7 @@ custom_target(
     output: 'glycin-thumbnailer',
     command: [
         'cp',
-        cargo_target_dir / rust_target / 'glycin-thumbnailer',
+        cargo_output,
         '@OUTPUT@',
     ],
 )
diff --git a/libglycin/meson.build b/libglycin/meson.build
index 209b641ac923..a21491716a01 100644
--- a/libglycin/meson.build
+++ b/libglycin/meson.build
@@ -19,6 +19,10 @@ cargo_options = [
     '--manifest-path', manifest,
 ]
 
+if get_option('triplet') != ''
+  cargo_options += [ '--target', get_option('triplet') ]
+endif
+
 cargo_env_module = cargo_env
 
 packages = []
@@ -70,7 +74,11 @@ foreach package : packages
         endif
     endif
 
-    cargo_output = cargo_target_dir / rust_target / f'libglycin@suffix@.so'.replace('-', '_')
+    if get_option('triplet') != ''
+      cargo_output = cargo_target_dir / get_option('triplet') / rust_target / f'libglycin@suffix@.so'.replace('-', '_')
+    else
+      cargo_output = cargo_target_dir / rust_target / f'libglycin@suffix@.so'.replace('-', '_')
+    endif
 
     # Build library with Rust
     libglycin = custom_target(
diff --git a/meson_options.txt b/meson_options.txt
index d2d368d0265c..5bbec397b4a3 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -15,6 +15,12 @@ option(
   value: 'release',
 )
 
+option(
+  'triplet',
+  type: 'string',
+  value: '',
+  description: 'Rust target triplet (if needed)')
+
 # glycin loaders
 
 option(
