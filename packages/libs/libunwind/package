description: Portable and efficient C programming interface (API) to determine the call-chain of a program
version: 1.6.2
license: GPL-2.0

depends:
  - devel/glibc
  - libs/xz

files:
  - download:
      url: https://download.savannah.nongnu.org/releases/libunwind/libunwind-${version}.tar.gz
      sha1: 1e4d5e4b9502bfbe7609a4a64b83eb040be3f741

packages:
  libunwind:
    files:
      - /usr/lib/*.so.*

  libunwind-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libunwind-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
