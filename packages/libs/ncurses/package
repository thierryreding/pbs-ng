include: packages/libs/ncurses/package.include

depends:
  - devel/glibc

packages:
  ncurses:
    files:
      - /usr/lib/*.so.*

  ncurses-bin:
    files:
      - /usr/bin

  ncurses-data:
    files:
      - /usr/share/tabset
      - /usr/share/terminfo

  ncurses-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  ncurses-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
