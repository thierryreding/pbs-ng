description: device mapper userspace library and tools
version: 2.02.181
license: GPL-2.0, LGPL-2.1

files:
  - download:
      url: ftp://sources.redhat.com/pub/lvm2/LVM2.${version}.tgz
      sha1: 2802799c60ef4f61534df1e40bcc29e4e043b29b

depends:
  - devel/glibc
  - libs/libaio

packages:
  libdevmapper:
    files:
      - /usr/lib/*.so.*

  libdevmapper-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
