description: A URL retrieval utility and library
version: 7.75.0
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://curl.haxx.se/download/curl-${version}.tar.xz
      sha1: ae65d2140104f441b65b60c5e1d541d11dab80c6

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
