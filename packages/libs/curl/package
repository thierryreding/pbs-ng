description: A URL retrieval utility and library
version: 7.61.1
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://curl.haxx.se/download/curl-${version}.tar.xz
      sha1: 8b56123714b4e061f0f71005c5be598b12f82483

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
