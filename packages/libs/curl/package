description: A URL retrieval utility and library
version: 7.61.0
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://curl.haxx.se/download/curl-${version}.tar.xz
      sha1: 9fda7fc5e94337a25166e885e9abe05e0adace53

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
