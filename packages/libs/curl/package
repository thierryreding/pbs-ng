description: A URL retrieval utility and library
version: 7.59.0
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://curl.haxx.se/download/curl-${version}.tar.xz
      sha1: f6ec54869dce33abcba4e6d919165fe3c9e1dff5

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
