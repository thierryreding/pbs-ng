description: A URL retrieval utility and library
version: 7.58.0
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://curl.haxx.se/download/curl-${version}.tar.xz
      sha1: edbc1a5a558b091e3c75987d26a4667ef20af84d

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
