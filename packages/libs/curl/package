description: A URL retrieval utility and library
version: 8.20.0
license: MIT

depends:
  - libs/libidn2
  - libs/libpsl
  - libs/openssl

files:
  - download:
      url: https://curl.se/download/curl-${version}.tar.xz
      sha1: 45177933b0feea3faabeffdb9496b163b15131be
      watch:
        url: anitya://381

packages:
  curl:
    files:
      - /usr/bin

  libcurl:
    files:
      - /usr/lib/*.so.*

  libcurl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 sts=2
