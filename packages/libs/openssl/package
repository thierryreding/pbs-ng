description: The Open Source toolkit for Secure Socket Layer and Transport Layer Security
version: 3.0.7
license: BSD

depends:
  - devel/glibc
  - libs/zlib

provides:
  - libs/ssl

files:
  - download:
      url: https://www.openssl.org/source/openssl-${version}.tar.gz
      sha1: f20736d6aae36bcbfa9aba0d358c71601833bf27

packages:
  openssl:
    files:
      - /etc/ssl
      - /usr/bin
      - /usr/lib/*.so.*
      - /usr/lib/ssl

  openssl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  openssl-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
