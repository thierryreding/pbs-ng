description: Simple DirectMedia Layer
version: 2.0.10
license: MIT

depends:
  - devel/glibc
  - libs/mesa

files:
  - download:
      url: https://www.libsdl.org/release/SDL2-${version}.tar.gz
      sha1: fb31312ea1d4b45db839796ae2336dfe3d5884e5

packages:
  libsdl:
    files:
      - /usr/lib/*.so.*

  libsdl-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
