description: General purpose cryptographic library based on the code from GnuPG
version: 1.8.2
license: GPL

depends:
  - devel/glibc
  - libs/libgpg-error

files:
  - download:
      url: ftp://ftp.gnupg.org/gcrypt/libgcrypt/libgcrypt-${version}.tar.bz2
      sha1: ab8aae5d7a68f8e0988f90e11e7f6a4805af5c8d

packages:
  libgcrypt:
    files:
      - /usr/lib/*.so.*

  libgcrypt-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig
      - /usr/share/aclocal

# vim: et ft=yaml sts=2 sw=2 ts=2
