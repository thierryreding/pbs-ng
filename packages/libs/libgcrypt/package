description: General purpose cryptographic library based on the code from GnuPG
version: 1.8.0
license: GPL

depends:
  - devel/glibc
  - libs/libgpg-error

files:
  - download:
      url: ftp://ftp.gnupg.org/gcrypt/libgcrypt/libgcrypt-${version}.tar.bz2
      sha1: b4ffb20369f2ab8249d5cc0fb8b3b31371f6b112

packages:
  libgcrypt:
    files:
      - /usr/lib/*.so.*

  libgcrypt-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig
      - /usr/share/aclocal

# vim: et ft=yaml sts=2 sw=2 ts=2
