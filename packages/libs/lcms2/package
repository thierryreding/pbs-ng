description: little CMS color engine
version: 2.12
license: MIT

depends:
  - devel/glibc

files:
  - download:
      url: https://prdownloads.sourceforge.net/lcms/lcms/${version}/lcms2-${version}.tar.gz
      sha1: 3d0c0276fcd3930b80c424512ec0b1ed54ec1497

packages:
  lcms2:
    files:
      - /usr/lib/*.so.*

  lcms2-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
