description: A library to decode several popular sound file formats such as .WAV and .MP3
version: 2.0.4
license: LGPL-2.1-or-later

depends:
  - libs/sdl

files:
  - download:
      url: https://github.com/icculus/SDL_sound/archive/refs/tags/v${version}.tar.gz
      sha1: ed40014383174447e30af5ac41233ce3e3b465ef
      watch:
        url: anitya://10262

packages:
  libsdl-sound:
    files:
      - /usr/lib/*.so.*

  libsdl-sound-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
