description: The Linux-native asynchronous I/O facility (aio) library
version: 0.3.111
license: LGPL

depends:
  - devel/glibc

files:
  - download:
      url: https://releases.pagure.org/libaio/libaio-${version}.tar.gz
      sha1: 6ec2b954831d35573fd7f38e2423304eb841b50c

packages:
  libaio:
    files:
      - /usr/lib/*.so.*

  libaio-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

# vim: et ft=yaml sts=2 sw=2 ts=2
