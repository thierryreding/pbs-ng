description: The Linux-native asynchronous I/O facility (aio) library
version: 0.3.113
license: LGPL

depends:
  - devel/glibc

files:
  - download:
      url: https://releases.pagure.org/libaio/libaio-${version}.tar.gz
      sha1: 86727941e0d3084ce264b8a6878961812bc212b8

packages:
  libaio:
    files:
      - /usr/lib/*.so.*

  libaio-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

# vim: et ft=yaml sts=2 sw=2 ts=2
