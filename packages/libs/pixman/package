include: packages/libs/pixman/package.include

depends:
  - devel/glibc

packages:
  pixman:
    files:
      - /usr/lib/*.so.*

  pixman-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  pixman-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
