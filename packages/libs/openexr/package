description: reference implementation of the EXR file format
version: 2.5.5
license: custom

depends:
  - devel/glibc

files:
  - download:
      url: https://github.com/AcademySoftwareFoundation/openexr/archive/v${version}.tar.gz
      sha1: f55bb2051b409fef1c983cbfa37c72be79038568

packages:
  openexr:
    files:
      - /usr/bin
      - /usr/lib/*.so.*

  openexr-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
