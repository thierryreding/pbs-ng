description: library for handling OpenGL function pointer management
version: 1.5.3
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/${version}/libepoxy-${version}.tar.xz
      sha1: e00d0bf270a99ded2a2f4abe7874b765cd338d1f

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
