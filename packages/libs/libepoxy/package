description: library for handling OpenGL function pointer management
version: 1.4.3
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/${version}/libepoxy-${version}.tar.xz
      sha1: e29ebdba0d1d4601431639331d2e6f39f180972d

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
