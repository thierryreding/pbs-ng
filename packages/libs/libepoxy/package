description: library for handling OpenGL function pointer management
version: 1.3.1
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/v${version}/libepoxy-${version}.tar.bz2
      sha1: 878599a610403d4d8732e7e61e382dfca8076ab6

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
