description: library for handling OpenGL function pointer management
version: 1.5.9
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/${version}/libepoxy-${version}.tar.xz
      sha1: 531d98d849ead728a0fc7722df5487a3d9108829

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
