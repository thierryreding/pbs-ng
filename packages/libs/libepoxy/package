description: library for handling OpenGL function pointer management
version: 1.5.0
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/${version}/libepoxy-${version}.tar.xz
      sha1: 7648b04a8c85d76db081be3272577dcab66bb6d2

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
