description: library for handling OpenGL function pointer management
version: 1.5.5
license: MIT

files:
  - download:
      url: https://github.com/anholt/libepoxy/releases/download/${version}/libepoxy-${version}.tar.xz
      sha1: 4036d242ad89b8b999b820d2c5093a380fe99d0b

depends:
  - libs/mesa

packages:
  libepoxy:
    files:
      - /usr/lib/*.so.*

  libepoxy-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
