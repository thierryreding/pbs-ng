include: packages/libs/pcre2/package.include

depends:
  - devel/glibc
  - libs/bzip2
  - libs/zlib

packages:
  libpcre2:
    files:
      - /usr/lib/*.so.*

  libpcre2-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

  libpcre2-man:
    files:
      - /usr/share/man/man3

  libpcre2-doc:
    files:
      - /usr/share/doc

# vim: et ft=yaml sts=2 sw=2 ts=2
