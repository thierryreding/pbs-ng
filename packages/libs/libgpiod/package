description: C library and tools for interacting with Linux GPIO character devices
version: 0.3
license: LGPL-2.1

depends:
  - devel/glibc

files:
  - download:
      url: https://github.com/brgl/libgpiod/archive/v${version}.tar.gz
      sha1: 1dd87f9fc59466ab5a51fc5ba95b6530d7d95bba

packages:
  libgpiod:
    files:
      - /usr/lib/*.so.*

  libgpiod-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

  libgpiod-tools:
    files:
      - /usr/bin

# vim: et ft=yaml sts=2 sw=2 ts=2
