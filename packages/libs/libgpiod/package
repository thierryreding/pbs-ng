description: C library and tools for interacting with Linux GPIO character devices
version: 1.6.3
license: LGPL-2.1

depends:
  - devel/glibc

files:
  - download:
      url: https://git.kernel.org/pub/scm/libs/libgpiod/libgpiod.git/snapshot/libgpiod-${version}.tar.gz
      sha1: 9be40cfaa5ef71ec619a8ad754c14d3a4511ad48

packages:
  libgpiod:
    files:
      - /usr/lib/*.so.*

  libgpiod-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

  libgpiod-tools:
    files:
      - /usr/bin

# vim: et ft=yaml sts=2 sw=2 ts=2
