description: C library and tools for interacting with Linux GPIO character devices
version: 1.4.1
license: LGPL-2.1

depends:
  - devel/glibc

files:
  - download:
      url: https://git.kernel.org/pub/scm/libs/libgpiod/libgpiod.git/snapshot/libgpiod-${version}.tar.gz
      sha1: 586a1254b9602adaca303f17dc7b7d2aa5aab4c9

packages:
  libgpiod:
    files:
      - /usr/lib/*.so.*

  libgpiod-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

  libgpiod-tools:
    files:
      - /usr/bin

# vim: et ft=yaml sts=2 sw=2 ts=2
