include $(TOP_SRCDIR)/packages/autotools.mk

conf-args += \
	--bindir=$(PREFIX)/bin \
	--sbindir=$(PREFIX)/bin \
	--disable-account-tools-setuid \
	--with-libpam \
	--without-selinux \
	--without-libbsd \
	--without-ncsd \
	--without-su
