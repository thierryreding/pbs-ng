description: basic filesystem hierarchy
version: 0
license: GPL-2.0

packages:
  base-files:
    files:
      - /boot
      - /dev
      - /etc
      - /home
      - /lib
      - /mnt
      - /opt
      - /proc
      - /root
      - /run
      - /srv
      - /sys
      - /tmp
      - /usr
      - /var

# vim: et ft=yaml sts=2 sw=2 ts=2
