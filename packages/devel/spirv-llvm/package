include: packages/devel/spirv-llvm/package.include

depends:
  - build-tools/cmake
  - devel/llvm
  - devel/spirv-tools

packages:
  spirv-llvm:
    files:
      - /usr/bin
      - /usr/lib/*.so.*

  spirv-llvm-dev:
    files:
      - /usr/include
      - /usr/lib/*.so
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
