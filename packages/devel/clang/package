include: packages/devel/clang/package.include

depends:
  - build-tools/clang
  - build-tools/cmake
  - devel/llvm

packages:
  clang:
    files:
      - /usr/bin
      - /usr/lib/*.so.*
      - /usr/libexec
      - /usr/share/clang
      - /usr/share/scan-build
      - /usr/share/scan-view

  clang-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/clang
      - /usr/lib/cmake

# vim: et ft=yaml sts=2 sw=2 ts=2
