include: packages/devel/flex/package.include

depends:
  - devel/glibc

packages:
  flex:
    files:
      - /usr/bin
      - /usr/lib/*.so.*

  flex-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so

  flex-doc:
    files:
      - /usr/share/doc
      - /usr/share/info
      - /usr/share/licenses

  flex-locale:
    files:
      - /usr/share/locale

  flex-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
