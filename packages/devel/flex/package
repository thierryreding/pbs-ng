description: A tool for generating text-scanning programs
version: 2.5.37
license: custom

files:
  - download:
      url: http://prdownloads.sourceforge.net/flex/flex-${version}.tar.bz2
      sha1:

packages:
  flex:
    files:
      - /usr/bin

  flex-dev:
    files:
      - /usr/include
      - /usr/lib

  flex-doc:
    files:
      - /usr/share/doc
      - /usr/share/info
      - /usr/share/licenses

  flex-locale:
    files:
      - /usr/share/locale

  flex-man:
    files:
      - /usr/share/man

# vim: et ft=yaml sts=2 sw=2 ts=2
