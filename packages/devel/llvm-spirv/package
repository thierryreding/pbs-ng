description: LLVM/SPIR-V bi-directional translator
version: git
license: custom

depends:
  - devel/llvm

files:
  - source:
      url: git+https://github.com/pierremoreau/llvm-spirv.git#rebase2_integrate_with_mesa

packages:
  llvm-spirv:
    files:
      - /usr/bin
      - /usr/lib/*.so

  llvm-spirv-dev:
    files:
      - /usr/include
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
