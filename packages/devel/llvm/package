include: packages/devel/llvm/package.include

depends:
  - build-tools/cmake
  - build-tools/llvm
  - devel/glibc
  - libs/ncurses
  - libs/zstd

packages:
  llvm:
    files:
      - /usr/bin
      - /usr/lib/*.so.*

  llvm-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/*.so
      - /usr/lib/cmake

# vim: et ft=yaml sts=2 sw=2 ts=2
