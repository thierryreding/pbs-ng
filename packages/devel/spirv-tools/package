include: packages/devel/spirv-tools/package.include

depends:
  - build-tools/cmake
  - devel/glibc
  - devel/spirv-headers

packages:
  spirv-tools:
    files:
      - /usr/bin
      - /usr/lib/*.so

  spirv-tools-dev:
    files:
      - /usr/include
      - /usr/lib/*.a
      - /usr/lib/cmake
      - /usr/lib/pkgconfig

# vim: et ft=yaml sts=2 sw=2 ts=2
